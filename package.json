{
  "name": "batel-therapy-management",
  "version": "1.0.0",
  "private": true,
  "description": "Local-first therapy management app for Batel - Phase 1 MVP",
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "dev:backend": "npm --workspace backend run dev",
    "dev:frontend": "npm --workspace frontend run dev",
    "build": "npm --workspace frontend run build && npm --workspace backend run build",
    "start": "npm --workspace backend run start",
    "seed": "npm --workspace backend run prisma:seed"
  }
}
